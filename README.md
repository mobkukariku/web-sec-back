# Web Security Backend

Безопасный REST API бэкенд для приложения рецептов с реализацией современных мер защиты от уязвимостей веб-приложений.

## 🛡️ Меры безопасности

Проект реализует комплексную защиту от основных уязвимостей:

### ✅ Защита от SQL-инъекций
- Все SQL-запросы используют параметризованные запросы (prepared statements)
- Никакие пользовательские данные не вставляются напрямую в SQL
- Использование параметров `$1`, `$2`, и т.д. для всех запросов

### ✅ Защита от XSS (Cross-Site Scripting)
- Включены security headers через Helmet
- Санитизация HTML-вывода (экранирование специальных символов)
- Content Security Policy (CSP) настроен
- Безопасная обработка пользовательского контента

### ✅ JWT-аутентификация
- Токен-базированная аутентификация вместо передачи userId
- Токены с истечением срока действия (24 часа)
- Middleware для проверки токенов на защищенных маршрутах
- Безопасное хранение токенов на клиенте

### ✅ Валидация входных данных
- Валидация через `express-validator`
- Проверка типов данных, длины, форматов
- Санитизация email, строковых полей
- Валидация ID параметров

### ✅ Rate Limiting
- Общий лимит: 100 запросов за 15 минут с одного IP
- Строгий лимит для аутентификации: 5 попыток за 15 минут
- Защита от brute-force атак

### ✅ Дополнительные меры
- CORS настроен с указанием разрешенных origins
- Helmet для security headers
- Обработка ошибок авторизации (401/403)
- Защищенные маршруты требуют JWT токен

## 📋 Требования

- Node.js (версия 18 или выше)
- PostgreSQL (версия 12 или выше)
- npm или yarn

## 🚀 Установка

1. Установите зависимости:
```bash
npm install
```

2. Настройте базу данных PostgreSQL:
```bash
# Создайте базу данных
createdb mydb

# Или используйте docker-compose.yml для запуска PostgreSQL в Docker
docker-compose up -d
```

3. Создайте таблицы в базе данных (пример SQL):
```sql
-- Таблица пользователей
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    name VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица рецептов
CREATE TABLE recipes (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT NOT NULL,
    duration INTEGER NOT NULL,
    author_id INTEGER REFERENCES users(id) ON DELETE CASCADE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица ингредиентов
CREATE TABLE ingredients (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL UNIQUE
);

-- Таблица связи рецептов и ингредиентов
CREATE TABLE recipe_ingredients (
    recipe_id INTEGER REFERENCES recipes(id) ON DELETE CASCADE,
    ingredient_id INTEGER REFERENCES ingredients(id) ON DELETE CASCADE,
    amount INTEGER NOT NULL,
    PRIMARY KEY (recipe_id, ingredient_id)
);
```

4. Настройте переменные окружения (создайте `.env` файл):
```env
# База данных
DB_USER=admin
DB_PASSWORD=admin
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb

# JWT
JWT_SECRET=your-secret-key-change-in-production

# Сервер
PORT=3002
FRONTEND_URL=http://localhost:5173
```

## 🏃 Запуск

### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

### Продакшн режим:
```bash
npm start
```

Сервер запустится на порту 3002 (или порту, указанному в переменных окружения).

## 📁 Структура проекта

```
web-sec-back/
├── config/              # Конфигурационные файлы
│   ├── app.js          # Настройки приложения
│   └── database.js      # Настройки подключения к БД
├── controllers/         # Контроллеры (бизнес-логика)
│   ├── authController.js
│   ├── profileController.js
│   ├── recipeController.js
│   ├── ingredientController.js
│   └── searchController.js
├── middleware/          # Middleware функции
│   ├── auth.js         # JWT аутентификация
│   └── security.js     # Валидация и санитизация
├── models/              # Модели данных (работа с БД)
│   ├── UserModel.js
│   ├── RecipeModel.js
│   └── IngredientModel.js
├── routes/              # Маршруты API
│   ├── authRoutes.js
│   ├── profileRoutes.js
│   ├── recipeRoutes.js
│   ├── ingredientRoutes.js
│   └── searchRoutes.js
├── index.js             # Точка входа приложения
├── package.json
└── README.md
```

## 🔌 API Endpoints

### Аутентификация

#### POST `/api/register`
Регистрация нового пользователя
```json
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "password": "password123"
}
```
**Ответ:**
```json
{
  "message": "Пользователь зарегистрирован",
  "token": "jwt-token-here",
  "userId": 1
}
```

#### POST `/api/login`
Вход в систему
```json
{
  "email": "ivan@example.com",
  "password": "password123"
}
```
**Ответ:**
```json
{
  "message": "Успешный вход",
  "token": "jwt-token-here",
  "userId": 1
}
```

### Рецепты

#### GET `/api/recipes`
Получить все рецепты (публичный)
**Query параметры:**
- `search` - поиск по названию или описанию

#### GET `/api/recipes/:id`
Получить рецепт по ID (публичный)

#### GET `/api/recipes/:id/html`
Получить рецепт в формате HTML (публичный)

#### POST `/api/recipes` 🔒
Создать новый рецепт (требует аутентификации)
```json
{
  "title": "Борщ",
  "description": "Традиционный украинский суп",
  "duration": 120,
  "ingredients": [
    {"id": 1, "amount": 500},
    {"id": 2, "amount": 300}
  ]
}
```

#### PUT `/api/recipes/:id` 🔒
Обновить рецепт (требует аутентификации)

#### GET `/api/recipes/my-recipes/me` 🔒
Получить мои рецепты (требует аутентификации)

### Профиль

#### GET `/api/profile/me` 🔒
Получить свой профиль (требует аутентификации)

#### PUT `/api/profile/me` 🔒
Обновить свой профиль (требует аутентификации)
```json
{
  "name": "Новое имя",
  "email": "newemail@example.com",
  "password": "newpassword" // опционально
}
```

### Ингредиенты

#### GET `/api/ingredients`
Получить все ингредиенты (публичный)

### Поиск

#### GET `/api/search?q=запрос`
Поиск (публичный)

#### POST `/api/comments`
Создать комментарий (публичный)

**🔒 - Требует JWT токен в заголовке Authorization: `Bearer <token>`**

## 🔐 Безопасность

### JWT Токены
- Токены генерируются при регистрации и входе
- Срок действия: 24 часа
- Передаются в заголовке: `Authorization: Bearer <token>`

### Защищенные маршруты
Все маршруты, помеченные 🔒, требуют валидный JWT токен. При отсутствии или невалидности токена возвращается ошибка 401 или 403.

### Валидация
Все входные данные валидируются:
- Email должен быть валидным email адресом
- Пароль минимум 6 символов
- Название рецепта: 1-200 символов
- Описание: 1-5000 символов
- Длительность: положительное число

## 🧪 Тестирование

Для тестирования API можно использовать:
- Postman
- curl
- HTTP клиенты (Insomnia, Thunder Client)

Пример запроса с токеном:
```bash
curl -X GET http://localhost:3002/api/profile/me \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## 🐳 Docker

Для запуска PostgreSQL через Docker:
```bash
docker-compose up -d
```

Это запустит PostgreSQL на порту 5432 с настройками:
- Пользователь: admin
- Пароль: admin
- База данных: mydb

## 📝 Заметки

- В продакшн окружении обязательно измените `JWT_SECRET` на сложный случайный ключ
- Настройте правильный `FRONTEND_URL` для CORS
- Используйте переменные окружения для всех секретных данных
- Рекомендуется использовать HTTPS в продакшн

## 📄 Лицензия

ISC

## 👤 Автор

Web Security Final Project
